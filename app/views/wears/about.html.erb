<div class="col text-right">
    <%= render 'layouts/user-nav' %>
</div>
<div class="container">
    <div class="row">
        <div class="col-lg-12">
            <% @wears.each do |wear| %>
                <div class="col-lg-5">
                    <% if wear.images.attached? %>
                        <div class="slider1">
                            <% wear.images.each do |image| %>
                                <div class="admin-index-image">
                                    <%= image_tag image.variant(combine_options:{resize:"200x200^",crop:"200x200+0+0",gravity: :center}).processed %>
                                </div> 
                            <% end %>
                        </div>
                    <% end %>
                </div>
                <div class="col-lg-4 wear-text">
                    <h4 class="text-centetr">
                        圧倒的着やすや、着心地を徹底的に追求して作りあげた一枚<br>
                        大きめでシンプルなデザイン、これであなたも最強の仲間入り
                    </h4>
                </div>
            <% end %>
        </div>
    </div>
</div>
<script>
  $(document).on('turbolinks:load', function(){
    $('.slider1').slick({
        slidesToShow: 1,
        slidesToScroll: 1,
        infinite: true,
        arrows: true,
        dots: true,
        arrows: false,
      });
  });
</script>